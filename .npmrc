shamefully-hoist=true
# Ensure optional dependencies are installed (fixes Rollup native binaries)
optional=true
# Retry failed optional dependencies
fetch-retries=3
fetch-retry-factor=10
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000
# Ignore scripts by default to avoid problematic postinstall scripts
ignore-scripts=true
